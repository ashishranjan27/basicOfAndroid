package com.example.recycleview_cardview.Models

class ReceipeModel(//We will use setter and getter in adapter
        //Is used for layout to get and set the data
        //Note image is always stored in binary form only
        var pic: Int, var text: String
)

